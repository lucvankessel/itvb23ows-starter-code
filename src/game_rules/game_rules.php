<?php

/**
 * Checks if the given move is valid or not.
 * It will throw an exception with the error that should be displayed to the user.
 *
 * @param [type] $board
 * @param [type] $piece
 * @param [type] $to
 * @return boolean
 */
function is_valid_move($board, $piece, $to): bool {
    return true;
}

/**
 * Checks if the given play is valid.
 * It will throw an exception with the error that should be displayed to the user.
 *
 * @param [type] $board
 * @param [type] $piece
 * @param [type] $to
 * @return boolean
 */
function is_valid_play($board, $piece, $to): bool {
    return true;
}